node('SLAVE') {
  /* stage('run-pipeline') {
    build job: 'Other/gbarata-web-ui-pipeline-test', parameters: [[$class: 'StringParameterValue', name: 'repository', value: 'nuxeo-elements'], [$class: 'StringParameterValue', name: 'branch', value: env.BRANCH_NAME]], quietPeriod: 3
  } */
  echo 'Everything is fine!'
}
